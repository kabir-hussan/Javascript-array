//show the value of an array using for of and for loop


const friends=['Rahim', 'Karim', 'Fahim', 'Lahim'];

for(const friend of friends)
{

    console.log(friend);
}

// For loop for showing array item

console.log("\nShow the array item using for loop:");

const books=['Bengali', 'English', 'Math', 'Physics', 'Chemistry'];

for(let i=0; i<books.length; i++)
{

    console.log(books[i]);
}


console.log("\nShow the numbers items using for loop: ");

const numbers=[11, 12, 22, 33, 44, 55, 66];

for(let i=0; i<numbers.length; i++)
{

    console.log(numbers[i]);
}